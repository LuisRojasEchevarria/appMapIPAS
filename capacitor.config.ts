import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pe.com.fondepes',
  appName: 'Desembarcaderos Fondepes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
